# name: test/sql/radio.test
# description: test radio extension
# group: [radio]

# Before we load the extension, this will fail
statement error
SELECT radio('Sam');
----
Catalog Error: Scalar Function with name radio does not exist!

# Require statement will ensure this test is run with this extension loaded
require radio

# Confirm the extension works
query I
SELECT radio('Sam');
----
Quack Sam 🐥

query I
SELECT radio_openssl_version('Michael') ILIKE 'Quack Michael, my linked OpenSSL version is OpenSSL%';
----
true
